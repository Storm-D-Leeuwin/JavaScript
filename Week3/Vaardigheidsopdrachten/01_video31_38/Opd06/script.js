function som (one, two) {
    if (one > two){
        console.log(`${one} is groter dan ${two}`);
    } else if (two > one) {
        console.log(`${two} is groter dan ${one}`);
    }
}

console.log(som(12,4));
console.log(som(5,7));

